// Destructuring most use in REACT 

const data=[10,20,30,40]; 
let [x,y,z,w]=data; 
console.log(data)

const studentIndfo={
    name: "ravi",
    clas: "2nd", 
    roll_no: 5,
}
let {name,clas,roll_no}=studentIndfo; 
// console.log(name,clas,roll_no);
console.log(studentIndfo)